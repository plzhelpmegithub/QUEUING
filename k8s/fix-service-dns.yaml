apiVersion: v1
kind: Service
metadata:
  name: backend-counter-service
  namespace: default
  labels:
    app: backend-counter
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  selector:
    app: backend-counter
